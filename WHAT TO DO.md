# WHAT TO DO — the GitHub push, 2026-09-16 (website upload)

Repo: github.com/Gap-geometry/sqrt2-ln2-geometric-constants-  ·  everything in this folder mirrors the repo's layout.

1  On the repo page: Add file → Upload files. Drag the CONTENTS of this folder (not the folder itself): the root
   pages, papers/, visualisations/ (the Lock Atlas inside it, unlisted), lock-atlas.html (a forward from the address a paper prints). GitHub creates the two folders. One commit message:
   "September 2026: the two new papers, the thirteen reprints (papers/), the instruments (visualisations/), the whites".
   index.html is replaced in the same commit as everything else, so the root is never missing.
2  Nothing at the root needs deleting: the old paper and instrument files left the root on 2026-09-11.
   (archive/ and Hall of Memories/ stay as they are.)
3  After the commit: Google Search Console → resubmit sitemap.xml → Request indexing for the root and about.html.
4  Then MASTER_LINKS and Publication Dates (Keel, from the ledger), and the OSF descriptions' framework links
   are already right (the four anchors).
What is NOT in this push: the six papers whose OSF re-upload waits (their OSF file links stay grey on the
Documents page, DOI and project link live); Resistance and the other workshop pages (not in the selection).
